<%= form_for(@event_user_status) do |f| %>
  <% if @event_user_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_user_status.errors.count, "error") %> prohibited this event_user_status from being saved:</h2>

      <ul>
      <% @event_user_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
