<%= form_for(@venue_user) do |f| %>
  <% if @venue_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_user.errors.count, "error") %> prohibited this venue_user from being saved:</h2>

      <ul>
      <% @venue_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
